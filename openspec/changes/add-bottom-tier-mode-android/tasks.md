# Tác vụ: Thêm Chế độ Bottom Tier vào Android

- [x] Cập nhật enum `Mode` trong `BotKeyboardService.kt`.
- [x] Cập nhật hàm `updateUI` để hỗ trợ hiển thị Mode mới.
- [x] Cập nhật logic chọn từ trong `pickOneWord` và `pickTwoWord` để xử lý `BOTTOM_TIER`.
- [x] Tạo drawable `badge_bottom.xml` cho Badge màu cam.
- [ ] Kiểm tra việc chuyển đổi giữa 3 chế độ trên bàn phím thực tế.
- [ ] Đồng bộ code từ bản Android sang bản Private.
